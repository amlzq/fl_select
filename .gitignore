# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# Flutter/Dart/Pub related
**/doc/api/
.dart_tool/
build/

# The ROOT pubspec.lock must be committed (pub workspace requirement) —
# workspace members don't produce their own lock files. The example app
# (not a workspace member) keeps and commits its own lock.
packages/*/pubspec.lock
