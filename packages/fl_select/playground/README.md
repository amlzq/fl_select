# fl_select Playground

fl_select 的交互式调试台（Playground），从 `example/lib/playground` 迁移而来。

在手机框架中实时预览 `SelectView` / `PopupSelectBar` / `PopupSelectButton` 等组件，
并通过控制面板动态调整行为型与外观型参数，同时提供 Zillow / 乐有家两套房源筛选数据源用于测试。

## 运行

```sh
cd packages/fl_select/playground
flutter pub get
flutter run
```
